namespace Landis.PlugIns.Admin
{
	/// <summary>
	/// A method for checking if an extension name is already in use in the
	/// plug-in dataset.
	/// </summary>
	public delegate bool IsNameInUseMethod(string name);
}
